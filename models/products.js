class Products {
    constructor(
        id,
        userId,
        title,
        imageUrl,
        description,
        price
    ) {
        this.id = id
        this.userId = userId
        this.title = title
        this.imageUrl = imageUrl
        this.description = description
        this.price = price
    }
}

export default Products