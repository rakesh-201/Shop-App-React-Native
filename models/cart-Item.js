class Items {
    constructor(
        id,
        title,
        price,
        quantity,
        sum,
    ) {
        this.id = id
        this.title = title
        this.price = price
        this.quantity = quantity
        this.sum = sum
    }
}

export default Items